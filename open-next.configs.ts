import type { OpenNextConfig } from '@opennextjs/cloudflare';

const config: OpenNextConfig = {
  // 默认配置，可以根据项目需求进行调整
  // 更多配置选项可以参考OpenNext文档
  cloudflare: {
    // 可以在这里添加Cloudflare特定的配置
  }
};

export default config;